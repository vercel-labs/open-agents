import { deepAgent, deepAgentModelId } from "@open-harness/agent";

// Configure the agent here - single source of truth for the web app
export const webAgent = deepAgent;
export const webAgentModelId = deepAgentModelId;
